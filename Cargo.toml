[workspace]
members = [
    "programs/decentralized-roulette",
    "programs/decentralized-lottery"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
panic = "abort"

# Workspace-level dependency resolution
[workspace.dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }
anchor-spl = "0.30.1"
solana-program = "1.16.0"
spl-token = { version = "3.5.0", features = ["no-entrypoint"], default-features = false }
spl-associated-token-account = "1.1.2"
# switchboard-solana = "0.30.4"  # Latest Switchboard SDK compatible with Anchor 0.30.x - temporarily disabled for testing
# tuktuk-sdk = { git = "https://github.com/helium/tuktuk.git", branch = "main" }  # Add TukTuk SDK - disabled due to version conflict