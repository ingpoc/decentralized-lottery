[workspace]
members = [
    "programs/decentralized-lottery",
    "programs/decentralized-roulette"
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
panic = "abort"

# Remove workspace-level anchor-lang dependency to avoid conflicts
